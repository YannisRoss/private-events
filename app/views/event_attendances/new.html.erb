<h1><%= current_user.name%> attend <%= @event.title %></h1>

<%= form_with(model: @event_attendance) do |form| %>
  <% if @event_attendance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_attendance.errors.count, "error") %> prohibited this @event_attendance from being saved:</h2>

      <ul>
        <% @event_attendance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= form.hidden_field :user_id, value: current_user.id %>
<%= form.hidden_field :event_id, value: @event.id %>



<br><br>

  <div class="actions">
  By clicking the button below, I confirm I am attending this event.<br>
    <%= form.submit %>
  </div>
<% end %>
